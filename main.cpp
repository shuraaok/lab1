#include <iostream>
#include <math.h>
using namespace std;

int main() {
    int a, b, c, d;
    cin >> a >> b;
    c = sqrt(a*a + b*b);
    d = a + b + c;
    cout << c << endl;
    cout << d << endl;
}
